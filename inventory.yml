all:
  children:
    server:
      hosts:
    ### Uncomment NSM if you are using a remote machine and fill in all the variables ###
        # NSM:
        #   ansible_host: x.x.x.x
        #   ansible_user: username
        #   ansible_ssh_private_key_file: /path/to/key
    ### Uncomment localhost if you are installing locally ###
        # localhost:
        #   ansible_connection: local